import Vue from 'vue'
import { Button, Cell, NavBar, Uploader, Field, Row, Search, Loading, Checkbox, CheckboxGroup } from 'vant'

Vue
  .use(Button)
  .use(Cell)
  .use(NavBar)
  .use(Uploader)
  .use(Field)
  .use(Row)
  .use(Search)
  .use(Loading)
  .use(Checkbox)
  .use(CheckboxGroup)
